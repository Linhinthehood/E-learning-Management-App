name: e_learning_management_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.10+1
  fl_chart: ^0.70.0
  carousel_slider: ^5.0.0

  # Firebase
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.5.2
  firebase_storage: ^12.3.8

  # Offline Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # State Management
  flutter_riverpod: ^2.6.1

  # Utilities
  path_provider: ^2.1.5
  intl: ^0.19.0
  file_picker: ^8.1.6
  connectivity_plus: ^6.1.2
  image_picker: ^1.1.2
  http: ^1.2.2
  url_launcher: ^6.3.1

  # CSV Export
  csv: ^5.0.0
  
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13

flutter:
  uses-material-design: true
